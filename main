include <stdio.h>
include <stdlib.h>
include <string.h>
include <file.h>
include <iostream>
include <vector.h>

using namespace std ;

struct Type {
	int num ;
	Type *next ;
} // Type()

strcut Data {
	Type type ;
	string name ;
	int credit ;
	// string semester ;
} ; // Data()

class DataVector {
	vector datas ;
	
  public :
	void Read() ;
	void Add() ;
	void Sort() ;
	void Write_passed() ;
	void Write_course() ; // 覆寫
	void Show() ;
} // class DataVector

int main() {
/*
	read
	while
	  	funtion
			(a:add), (s:show), (q:quit)
		if a // add a input q to quit,double while loop
			cout()
			do-while?
			cin type // remember new a int pointer
				if type == 0-4
					if typr -> next == ..... // remember new
			cin name
			cin credit
			
			if q
				calculation
		if s // shile	
*/	
} // main()
